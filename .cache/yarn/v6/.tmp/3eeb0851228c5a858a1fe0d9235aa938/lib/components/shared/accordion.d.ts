import * as React from 'react';
interface Props {
    title: string;
    defaultExpanded: boolean;
    square?: boolean;
    children?: React.ReactNode;
}
export declare function LeftAccordion(props: Props): JSX.Element;
export {};
